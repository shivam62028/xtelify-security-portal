# Xtelify Outlook Desktop Helper — Python Dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Or use the provided install_and_run.bat on Windows (recommended).

flask>=3.0.0
requests>=2.31.0
pywin32>=306
